name: Second-ci
on: [push]
jobs:
  using-checkout-cmd:
    runs-on: ubuntu-latest
    steps:
        
      - name: some-script
        run: |
         echo 
           sudo apt-get install java -y 
           java --version 
        shell: bash

