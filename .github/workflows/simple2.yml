name: Second-ci
on: [push]
jobs:
  using-checkout-cmd:
    runs-on: ubuntu-latest
    steps:
      - name: using-checkout
        uses: Navaneeth-Kalkeri/Actions1
      - name: some-script
        run: |
         echo 
           pwd
           ls 
           top
           grep 


